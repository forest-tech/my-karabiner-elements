# my-karabiner-elements

## 環境

macOS(JIS配列)

## ファイル

- `emacs_keybindings.json`: Emacsのキーバインド関係の設定．基本的にはEmacsの操作と普通の操作のズレ(Ctrl+m と Enter のズレなど)を補正する．
- `meh_numlayer.json`: Mehキー(Ctrl + Option + Shift)を使って数字と記号を打つ用のレイヤーを実装する．
 
